<table>
  <tr>
    <th>Title</th>
    <th>Summary</th>
    <th>Body</th>
  </tr>
 
  <% @posts.each do |post| %>
    <tr>
      <td><%= link_to  "#{post.title}", post_path(post) %> </td>
      <td><%= post.summary %></td>
      <td><%= post.body %></td>
      
    </tr>
  <% end %>
</table>